{
  "name": "mano-ordai-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "serve -p 3000",
    "build": "mkdir -p dist && cp index.html dist/",
    "start": "serve -p 3000"
  },
  "devDependencies": {
    "cheerio": "^1.2.0",
    "jsdom": "^30.0.1",
    "puppeteer": "^25.9.0",
    "serve": "^14.2.4"
  }
}
